

const Button = ({styles}) => {
  return (
    <button type="button" className={`py-4 px-6 bg-blue-gradient font-poppins font-medium rounded text-[18px] text-primary outline-none hover:scale-105 ${styles}`}>
      Get Started 
    </button>
  )
}

export default Button;






